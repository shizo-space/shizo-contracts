{
  "name": "shizo",
  "version": "0.1.0",
  "description": "Shizo Near Contracts",
  "author": "Shzio",
  "license": "MIT",
  "scripts": {
    "build": "cd nft-contract && ./build.sh && cd .. && ls && cd market-contract && ./build.sh && cd .."
  }
}
